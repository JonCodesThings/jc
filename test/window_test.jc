include "lib";
import gresslib;

i32 main()
{
	mut window_descriptor wnd_desc;
	wnd_desc.title = "hello jc";
	wnd_desc.width = (u32)640;
	wnd_desc.height = (u32)480;
	wnd_desc.style = window_style::WINDOW_BORDERED;

	wnd := create_window(&wnd_desc);

	x := -10;

	cont := process_os_events(wnd);

	while (cont == (i8)true)
	{
		cont = process_os_events(wnd);
	}

	destroy_window(wnd);

	return 0;
}