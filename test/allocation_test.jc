import io;
import memory;
include "stdlib";
link "libcmt.lib";

main()
{
	printf("big thunk\n");
	defer printf("bigger thunk\n");

	file := fopen("oof.txt", "r");
	defer printf("%p\n", file);
	defer fclose(file);
	defer printf("%p\n", file); 

	

	byte := alloc(1);
	printf("Allocated memory pointer: %p\n", byte);
	byte = dealloc(byte);
	printf("Deallocated memory pointer: %p\n", byte);

	return 0;
}
